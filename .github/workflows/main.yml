name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  push:
    paths: ['.github/workflows/*.yml']

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Khởi động hệ thống
        run: |
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 20
          echo "✅ Hệ thống đã sẵn sàng | $(date +'%H:%M:%S')"

      - name: Cập nhật proxy
        run: |
          # Thiết lập Git
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          
          # Biến đếm
          COUNT=0

          while true; do
            COUNT=$((COUNT+1))
            
            # Đồng bộ repo
            git reset --hard HEAD
            git clean -fd
            git pull origin main || {
              echo "⚠️ Lỗi khi pull, thử lại..."
              sleep 5
              git pull origin main
            }

            echo "\n⏱️ Lần $COUNT - $(date +'%H:%M:%S %d/%m/%Y')"
            echo "🔷 KẾT QUẢ TẢI PROXY:"
            
            # Tải proxies với xử lý lỗi
            mkdir -p Proxies
            for t in http https socks4 socks5; do
              if curl -m 15 -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt"; then
                count=$(grep -cve '^\s*$' "Proxies/$t.txt" 2>/dev/null || echo 0)
                echo "  ▸ $t: $count proxy"
              else
                echo "  ▸ $t: ❌ Không tải được"
                continue  # Bỏ qua nếu có lỗi
              fi
            done

            # Xử lý Git
            git add -A
            changes=$(git diff --name-only --cached)
            
            if [ -n "$changes" ]; then
              echo "\n📝 File thay đổi:"
              echo "$changes" | while read f; do echo "  • $f"; done
              
              git commit -m "🔄 Update $(date +'%H:%M %d/%m') - Lần $COUNT" || echo "⚠️ Không commit được"
              
              # Push với retry
              for i in {1..3}; do
                if git push origin main; then
                  echo "\n✅ Cập nhật thành công!"
                  break
                else
                  echo "\n🔄 Thử lại push ($i/3)..."
                  git pull --rebase origin main
                  sleep $i
                fi
              done
            else
              echo "\n⏩ Không có thay đổi"
            fi
            
            sleep 60
          done

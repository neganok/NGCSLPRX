name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Start service
        run: |
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 10

      - name: Update proxies
        run: |
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          
          # Tạo thư mục Proxies nếu chưa có
          mkdir -p Proxies
          
          # Lấy tất cả loại proxy
          for t in http https socks4 socks5; do
            curl -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt" || true
          done
          
          # Chỉ thêm file cần thiết
          git add Proxies/ data.db
          
          # Nếu có thay đổi thì commit và push
          if ! git diff --cached --quiet; then
            git commit -m "🔄 Update proxies $(date +'%H:%M')"
            git pull origin main
            git push origin main
            echo "✅ Đã cập nhật thành công"
          else
            echo "⏭ Không có thay đổi"
          fi

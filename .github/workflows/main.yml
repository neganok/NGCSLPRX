name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          clean: true

      - name: Khởi động hệ thống
        run: |
          # Reset toàn bộ repo
          git reset --hard HEAD
          git clean -fdx
          git pull --force origin main
          
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 15
          echo "🟢 Hệ thống sẵn sàng | $(date +'%H:%M:%S')"

      - name: Cập nhật tự động
        run: |
          # Thiết lập Git chống lỗi
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          git config --global gc.auto 0
          git config --global pack.threads 1

          while true; do
            # Header
            echo "\n═════════ $(date +'%H:%M:%S %d/%m/%Y') ═════════"
            
            # Đồng bộ repo cực mạnh
            git fetch --prune --progress
            git reset --hard origin/main
            git clean -fdx
            
            # Tải proxies
            echo "🔷 TẢI PROXY"
            mkdir -p Proxies
            for t in http https socks4 socks5; do
              echo "  ▸ $t:"
              if curl -m 15 -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt"; then
                count=$(grep -cve '^\s*$' "Proxies/$t.txt" 2>/dev/null || echo 0)
                echo "    ➤ $count proxy"
              else
                echo "    ➤ Lỗi tải"
              fi
            done

            # Xử lý Git siêu chắc chắn
            git add -A
            changes=$(git diff --name-only --cached)
            
            if [ -n "$changes" ]; then
              echo "\n🔷 THAY ĐỔI:"
              echo "$changes" | while read file; do
                echo "  • $file"
              done
              
              git commit -m "🔄 Update $(date +'%H:%M %d/%m')" || true
              
              echo "\n🔷 PUSH:"
              for i in {1..3}; do
                git fetch --prune
                if git push origin main; then
                  echo "  ✅ Thành công"
                  break
                else
                  echo "  🔄 Thử lại $i/3..."
                  git reset --hard origin/main
                  sleep $i
                fi
              done
            else
              echo "\n⏩ Không có thay đổi"
            fi
            
            echo "\n⏳ Chờ 60s..."
            sleep 60
          done

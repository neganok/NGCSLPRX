name: Proxy Master Final

on:
  schedule:
    - cron: '0 */5 * * *'
  push:
    paths: ['.github/workflows/*.yml']

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Khởi động
        run: |
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 15

      - name: Cập nhật
        run: |
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          git config --global pull.rebase false  # Luôn dùng merge

          while true; do
            # Tải proxies
            mkdir -p Proxies
            for t in http https socks4 socks5; do
              curl -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt" || true
            done

            # Xử lý git
            git add -A
            if ! git diff --cached --quiet; then
              git commit -m "📌 $(date +'%H:%M %d/%m')"
              git pull origin main  # Luôn merge
              git push origin main
              echo "✅ Đã push: $(git diff --name-only HEAD~1 HEAD)"
            fi
            
            sleep 60
          done

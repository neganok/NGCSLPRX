name: Proxy Master (Auto Update)

on:
  schedule:
    - cron: '0 */5 * * *'  # Chạy mỗi 5 giờ
  workflow_dispatch:        # Cho phép chạy thủ công

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Quan trọng để tránh lỗi git

      - name: 🚀 Khởi chạy Proxy Service
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt
          pkill -f "python3 start.py" || true
          nohup python3 start.py > proxy.log 2>&1 &
          sleep 10

      - name: ⚙️ Cấu hình Git chống lỗi
        run: |
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          git config --global http.postBuffer 524288000
          git config --global gc.auto 0  # Tắt garbage collection
          git config --global core.autocrlf false

      - name: 🔄 Cập nhật dữ liệu
        run: |
          START_TIME=$(date +%s)
          END_TIME=$((START_TIME + 5*60*60))  # 5 tiếng sau
          COUNTER=0
          
          while [ $(date +%s) -lt $END_TIME ]; do
            COUNTER=$((COUNTER+1))
            CURRENT_TIME=$(date '+%H:%M:%S')
            echo "🔄 Đang chạy - $CURRENT_TIME (Lần thứ $COUNTER)"
            
            # Tải proxy
            mkdir -p Proxies
            for type in http https socks4 socks5; do
              curl -s "http://127.0.0.1:8000/proxy/$type?time=3&minutes=15&format=text" -o "Proxies/$type.txt" || true
            done

            # Xử lý Git cực kỳ ổn định
            git reset --hard HEAD
            git clean -fd
            git pull origin main
            
            git add Proxies/ data.db  # Đã thêm data.db
            
            if ! git diff --cached --quiet; then
              git commit -m "📌 Update $CURRENT_TIME" --allow-empty || true
              
              # Push với retry
              for retry in {1..3}; do
                if git push origin main; then
                  echo "✅ Đã push thành công (Lần $COUNTER)"
                  break
                else
                  echo "🔄 Thử lại push ($retry/3)..."
                  sleep 5
                  git pull origin main
                fi
              done
            else
              echo "⏭️ Không có thay đổi"
            fi
            
            sleep 60
          done
          
          echo "🎉 Đã hoàn thành! Tổng số lần cập nhật: $COUNTER"

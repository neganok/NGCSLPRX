name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code (Optimized)
        uses: actions/checkout@v4
        with:
          # Bỏ fetch-depth để tránh treo
          ref: ${{ github.ref }}
          path: main-repo  # Thêm path để tránh xung đột

      - name: Cài đặt môi trường
        working-directory: ./main-repo  # Làm việc trong thư mục đã checkout
        run: |
          echo "🛠️ Đang cài đặt môi trường..."
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 10
          echo "✅ Cài đặt hoàn tất!"

      - name: Cập nhật tự động
        working-directory: ./main-repo
        run: |
          # Thiết lập git an toàn
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          git config --global gc.auto 0  # Tắt GC để tránh lỗi
          
          # Đồng bộ repo trước khi làm việc
          git remote update
          git reset --hard origin/main
          git clean -fd

          COUNT=0
          while true; do
            COUNT=$((COUNT + 1))
            echo "\n🔁 [LẦN $COUNT] Bắt đầu lúc $(date +'%H:%M:%S %d/%m/%Y')"
            
            # Tải proxies với hiển thị số lượng
            echo "📥 Tải proxies:"
            mkdir -p Proxies
            for t in http https socks4 socks5; do
              echo "  ↳ $t..."
              if curl -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt"; then
                count=$(wc -l < "Proxies/$t.txt")
                echo "     ✔️ $count proxy"
              else
                echo "     ❌ Lỗi"
              fi
            done

            # Xử lý git cực kỳ chắc chắn
            changed_files=$(git status -uno --porcelain)
            if [ -n "$changed_files" ]; then
              echo "📝 File thay đổi:"
              git diff --name-only --cached | while read file; do
                echo "  • $file"
              done

              echo "💾 Đang commit..."
              git add -A
              git commit -m "🔄 Update $(date +'%H:%M:%S %d/%m') [${COUNT}]"
              
              # Push với retry thông minh
              for i in {1..5}; do
                if git push origin main; then
                  echo "✅ Push thành công!"
                  break
                else
                  echo "🔄 Lỗi push (thử lại $i/5)..."
                  git pull --rebase origin main
                  sleep $i
                fi
              done
            else
              echo "⏩ Không có thay đổi"
            fi
            
            echo "⏳ Chờ 60s..."
            sleep 60
          done

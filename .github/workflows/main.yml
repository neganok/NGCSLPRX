name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  push:
    paths: ['.github/workflows/*.yml']

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Chạy hệ thống
        run: |
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 15

      - name: Cập nhật
        run: |
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          git config --global pull.rebase false  # Tắt rebase

          while true; do
            # Reset repo về trạng thái sạch
            git fetch origin
            git reset --hard origin/main
            
            # Tải proxies
            mkdir -p Proxies
            echo "🔄 Tải proxies:"
            for t in http https socks4 socks5; do
              if curl -m 10 -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt"; then
                count=$(grep -cve '^\s*$' "Proxies/$t.txt")
                echo "  ✅ $t: $count proxy"
              else
                echo "  ❌ $t: Lỗi"
              fi
            done

            # Xử lý git
            git add -A
            if git diff --cached --quiet; then
              echo "⏩ Không thay đổi"
            else
              git commit -m "📦 $(date +'%H:%M %d/%m')"
              git pull origin main  # Dùng merge thay vì rebase
              git push origin main && echo "✅ Đã lưu"
            fi
            
            sleep 60
          done

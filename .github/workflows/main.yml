name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  push:
    paths: ['.github/workflows/*.yml']

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Khởi động
        run: |
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 15

      - name: Cập nhật
        run: |
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          
          while true; do
            # Đồng bộ repo
            git pull origin main

            # Tải proxies
            mkdir -p Proxies
            echo "🔄 Đang cập nhật proxies:"
            for t in http https socks4 socks5; do
              curl -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt" && \
              echo "  ✅ $t: $(wc -l < Proxies/$t.txt) proxy" || echo "  ❌ $t: Lỗi"
            done

            # Xử lý thay đổi
            git add -A
            if git diff --cached --quiet; then
              echo "⏩ Không có thay đổi"
            else
              git commit -m "📦 Cập nhật lúc $(date +'%H:%M %d/%m')"
              git push origin main && echo "✅ Đã cập nhật thành công"
            fi
            
            sleep 60
          done

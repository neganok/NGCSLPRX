name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 300  # 5 giờ = 300 phút

    steps:
      - name: Clean workspace
        run: |
          sudo rm -rf *
          
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Start service
        run: |
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 10

      - name: Continuous updater
        run: |
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          mkdir -p Proxies

          # Bắt đầu đếm thời gian
          START_TIME=$(date +%s)
          END_TIME=$((START_TIME + 18000))  # 5 giờ = 18000 giây
          ITERATION=0

          while [ $(date +%s) -lt $END_TIME ]; do
            ITERATION=$((ITERATION + 1))
            echo "⏳ Lần cập nhật thứ $ITERATION"
            
            # Lấy proxies
            for t in http https socks4 socks5; do
              curl -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt" || true
            done

            # Kiểm tra thay đổi
            git add Proxies/ data.db
            
            if ! git diff --cached --quiet; then
              git commit -m "🔄 Update $(date +'%H:%M:%S')"
              
              # Push với retry
              for retry in {1..3}; do
                if git pull --rebase origin main && git push origin main; then
                  echo "✅ Đã push thành công"
                  break
                else
                  echo "🔄 Thử lại push (lần $retry/3)"
                  sleep 5
                  git reset --hard HEAD
                fi
              done
            else
              echo "⏭ Không có thay đổi"
            fi

            # Tính thời gian còn lại
            REMAINING=$((END_TIME - $(date +%s)))
            echo "⏱ Thời gian còn lại: $((REMAINING/60)) phút"
            
            sleep 60  # Chờ 60s
          done
          
          echo "🎯 Đã hoàn thành 5 giờ cập nhật"

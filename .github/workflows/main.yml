name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Khởi tạo môi trường
        run: |
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 10

      - name: Auto Update Proxy
        run: |
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"

          while true; do
            # Header
            echo "\n═════════ $(date +'%H:%M:%S %d/%m/%Y') ═════════"
            
            # Tải proxies
            echo "🔷 DANH SÁCH PROXY MỚI"
            mkdir -p Proxies
            for t in http https socks4 socks5; do
              if curl -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt"; then
                count=$(grep -cve '^\s*$' "Proxies/$t.txt")
                echo "  ▸ $t: $count proxy"
              else
                echo "  ▸ $t: ❌ LỖI"
              fi
            done

            # Xử lý Git
            git add -A
            changes=$(git diff --name-only --cached)
            
            if [ -n "$changes" ]; then
              echo "\n🔷 THAY ĐỔI ĐƯỢC PHÁT HIỆN"
              echo "$changes" | while read file; do
                echo "  • $file"
              done
              
              git commit -m "🔄 Cập nhật lúc $(date +'%H:%M %d/%m')" > /dev/null
              git push origin main && echo "\n✅ CẬP NHẬT THÀNH CÔNG!"
            else
              echo "\n⏩ Không có thay đổi"
            fi
            
            sleep 60
          done

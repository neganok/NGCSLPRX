name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # QUAN TRỌNG: Giải quyết lỗi unstable object data

      - name: Cài đặt môi trường
        run: |
          echo "🛠️ Đang cài đặt môi trường..."
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 10
          echo "✅ Cài đặt môi trường hoàn tất!"

      - name: Cập nhật tự động
        run: |
          echo "⚙️ Thiết lập thông tin Git..."
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          
          # Reset toàn bộ repo trước khi bắt đầu
          git reset --hard
          git clean -fd
          git pull origin main

          COUNT=0
          while true; do
            COUNT=$((COUNT + 1))
            echo "\n🔁 BẮT ĐẦU LẦN CHẠY THỨ $COUNT - $(date +'%H:%M:%S %d/%m/%Y')"
            
            # Tải proxies
            echo "📥 Đang tải proxies mới..."
            mkdir -p Proxies
            for t in http https socks4 socks5; do
              echo "  ↳ Đang tải $t..."
              curl -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt" || echo "  ❌ Lỗi khi tải $t"
              if [ -f "Proxies/$t.txt" ]; then
                count=$(wc -l < "Proxies/$t.txt")
                echo "  ➡️ Đã tải $count proxy $t"
              fi
            done
            
            # Xử lý Git
            echo "🔄 Đang kiểm tra thay đổi..."
            git add Proxies/ data.db
            
            # Kiểm tra chi tiết từng file thay đổi
            changed_files=$(git diff --cached --name-only)
            if [ -n "$changed_files" ]; then
              echo "📝 Phát hiện thay đổi các file:"
              for file in $changed_files; do
                echo "  • $file"
              done
              
              echo "💾 Đang tạo commit..."
              git commit -m "🔄 Cập nhật tự động lúc $(date +'%H:%M:%S %d/%m/%Y')"
              
              # Push với cơ chế retry mạnh mẽ
              echo "🚀 Đang đẩy thay đổi lên GitHub..."
              for attempt in {1..5}; do
                if git push origin main; then
                  echo "✅ Đẩy thành công sau $attempt lần thử!"
                  break
                else
                  echo "⚠️ Lỗi push (lần thử $attempt/5)"
                  git pull --rebase origin main
                  sleep $((attempt * 2))  # Tăng thời gian chờ mỗi lần thử
                fi
              done
            else
              echo "⏩ Không có thay đổi, bỏ qua commit"
            fi
            
            echo "⏳ Đợi 1 phút trước khi chạy lại..."
            sleep 60
          done

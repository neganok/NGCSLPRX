name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 300  # 5 tiếng

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Tải toàn bộ lịch sử git để tránh lỗi

      - name: Setup environment
        run: |
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 10

      - name: Simple updater
        run: |
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          
          # Reset và đồng bộ với remote trước khi bắt đầu
          git remote update
          git reset --hard origin/main
          git clean -fd
          
          COUNT=0
          while [ $COUNT -lt 300 ]; do
            COUNT=$((COUNT + 1))
            echo "🔄 Lần chạy thứ $COUNT"
            
            # Tải proxies
            mkdir -p Proxies
            for t in http https socks4 socks5; do
              curl -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt" || true
            done
            
            # Thêm thời gian chờ ngẫu nhiên để giảm xung đột
            sleep $((RANDOM % 10 + 1))
            
            # Thực hiện git operations với kiểm tra lỗi
            git add Proxies/ data.db
            if git diff --cached --quiet; then
              echo "⚠️ Không có thay đổi"
            else
              git commit -m "Cập nhật tự động $(date +'%H:%M:%S')"
              until git push origin main; do
                echo "⚠️ Lỗi khi push, thử lại sau..."
                git pull --rebase origin main
                sleep 5
              done
              echo "✅ Push thành công"
            fi
            
            sleep 60
          done
          
          echo "✅ Đã chạy đủ 5 tiếng"

name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  push:
    paths: ['.github/workflows/*.yml']

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Khởi động hệ thống
        run: |
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 15
          echo "✅ Hệ thống sẵn sàng | $(date +'%H:%M:%S')"

      - name: Cập nhật proxy
        run: |
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"

          while true; do
            # Reset repo trước mỗi lần chạy
            git reset --hard HEAD
            git clean -fd
            git pull origin main

            echo "\n⏱️ [$COUNT] $(date +'%H:%M:%S %d/%m/%Y')"
            echo "🔷 TẢI PROXY:"
            
            mkdir -p Proxies
            for t in http https socks4 socks5; do
              if curl -m 10 -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt"; then
                count=$(grep -cve '^\s*$' "Proxies/$t.txt")
                echo "  ▸ $t: $count proxy"
              else
                echo "  ▸ $t: ❌ Lỗi"
              fi
            done

            git add -A
            if git diff --cached --quiet; then
              echo "\n⏩ Không có thay đổi"
            else
              echo "\n📝 Thay đổi:"
              git diff --name-only --cached | while read f; do echo "  • $f"; done
              
              git commit -m "🔄 Update $(date +'%H:%M %d/%m')"
              git push origin main && echo "\n✅ Cập nhật thành công" || echo "\n⚠️ Lỗi khi push"
            fi
            
            sleep 60
          done

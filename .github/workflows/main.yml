name: Setup Proxy Machine

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '0 */5 * * *'

permissions:
  contents: write

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: 🛠️ Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 1
          persist-credentials: true

      - name: 🔄 Cài đặt hệ thống
        run: |
          sudo apt update && sudo apt install -y python3 python3-pip python3-venv curl netcat-traditional

      - name: 🏰️ Thiết lập môi trường
        run: |
          python3 -m venv env
          source env/bin/activate
          pip install --upgrade pip
          test -f requirements.txt && pip install -r requirements.txt

      - name: 🚀 Khởi động proxy
        run: |
          source env/bin/activate
          nohup python3 start.py > start.log 2>&1 &
          sleep 15

      - name: 🔑 Cấu hình Git
        run: |
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          git config --global init.defaultBranch main
          git config --global pull.rebase false

      - name: 🔄 Cập nhật và đồng bộ dữ liệu
        run: |
          set -e
          mkdir -p Proxies

          # Hàm tải proxy ổn định
          download_proxy() {
            local type=$1
            for i in {1..3}; do
              if curl -s --connect-timeout 15 "http://127.0.0.1:8000/proxy/${type}?time=2&minutes=10&format=text" -o "Proxies/${type}.txt" && [ -s "Proxies/${type}.txt" ]; then
                echo "✅ Đã tải $type"
                return 0
              fi
              sleep 5
            done
            echo "⚠️ Không thể tải $type"
            return 1
          }

          # Hàm đồng bộ Git
          sync_git() {
            # Thêm tất cả thay đổi
            git add -A
            
            if ! git diff --cached --quiet; then
              # Tạo commit
              git commit -m "🔄 Cập nhật proxy $(date +'%Y-%m-%d %H:%M:%S')" || true
              
              # Đồng bộ với remote
              for i in {1..3}; do
                if git pull origin main && git push origin main; then
                  echo "✅ Đã push thành công"
                  return 0
                fi
                echo "⚠️ Lỗi push, thử lại ($i/3)..."
                git reset --hard origin/main
                sleep 10
              done
              echo "❌ Không thể push sau 3 lần"
              return 1
            else
              echo "ℹ️ Không có thay đổi"
              return 0
            fi
          }

          # Vòng lặp chính
          for cycle in {1..300}; do
            echo "🔄 Chu kỳ $cycle/300"
            
            # Kiểm tra proxy server
            if ! nc -z -w 10 127.0.0.1 8000; then
              echo "❌ Proxy chưa sẵn sàng, chờ 30s..."
              sleep 30
              continue
            fi

            # Tải các proxy
            for type in http https socks4 socks5; do
              download_proxy "$type" || true
            done

            # Đồng bộ với Git
            if ! sync_git; then
              echo "⚠️ Bỏ qua lỗi đồng bộ, tiếp tục chu kỳ sau..."
            fi
            
            sleep 60
          done

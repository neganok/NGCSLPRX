name: Proxy Master

on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Khởi động hệ thống
        run: |
          # Xóa cache và reset repo
          git reset --hard HEAD
          git clean -fd
          git pull origin main
          
          # Cài đặt môi trường
          pip install -r requirements.txt
          nohup python3 start.py > start.log 2>&1 &
          sleep 20
          echo "🟢 Hệ thống sẵn sàng | $(date +'%H:%M:%S')"

      - name: Cập nhật tự động
        run: |
          # Thiết lập Git
          git config --global user.name "neganok"
          git config --global user.email "hackersvn1@gmail.com"
          git config --global gc.auto 0

          while true; do
            # Header
            echo "\n═════════ $(date +'%H:%M:%S %d/%m/%Y') ═════════"
            
            # Đồng bộ repo
            git pull origin main || {
              echo "🔴 Lỗi khi đồng bộ, thử lại..."
              git reset --hard HEAD
              git pull origin main
            }

            # Tải proxies với xử lý lỗi
            echo "🔷 TẢI PROXY"
            mkdir -p Proxies
            proxy_types=(http https socks4 socks5)
            
            for t in "${proxy_types[@]}"; do
              echo "  ▸ $t:"
              retry_count=0
              max_retries=2
              
              while [ $retry_count -lt $max_retries ]; do
                if curl -m 30 -s "http://127.0.0.1:8000/proxy/$t?time=3&minutes=15&format=text" -o "Proxies/$t.txt"; then
                  count=$(grep -cve '^\s*$' "Proxies/$t.txt" 2>/dev/null || echo 0)
                  if [ $count -gt 0 ]; then
                    echo "    ➤ $count proxy"
                    break
                  else
                    echo "    ➤ Cảnh báo: File rỗng"
                  fi
                else
                  retry_count=$((retry_count + 1))
                  echo "    ➤ Lỗi tải (thử lại $retry_count/$max_retries)"
                  sleep 5
                fi
              done
            done

            # Xử lý Git
            git add -A
            changes=$(git diff --name-only --cached)
            
            if [ -n "$changes" ]; then
              echo "\n🔷 THAY ĐỔI:"
              echo "$changes" | xargs -I{} echo "  • {}"
              
              echo "\n🔷 COMMIT:"
              git commit -m "🔄 Update $(date +'%H:%M %d/%m')" || echo "  ⚠️ Không commit được"
              
              echo "\n🔷 PUSH:"
              for i in {1..3}; do
                if git push origin main; then
                  echo "  ✅ Thành công"
                  break
                else
                  echo "  🔄 Thử lại $i/3..."
                  git pull --rebase origin main
                  sleep $i
                fi
              done
            else
              echo "\n⏩ Không có thay đổi"
            fi
            
            echo "\n⏳ Chờ 60s..."
            sleep 60
          done
